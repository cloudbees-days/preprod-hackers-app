apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer
on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:
  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['preprod-hackers-api']['hackers-api'].deploy }}
    uses: cloudbees-days/preprod-hackers-api/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['preprod-hackers-api']['hackers-api'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['preprod-hackers-api']['hackers-api'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['preprod-hackers-auth']['hackers-auth'].deploy }}
    uses: cloudbees-days/preprod-hackers-auth/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['preprod-hackers-auth']['hackers-auth'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['preprod-hackers-auth']['hackers-auth'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['preprod-hackers-web']['hackers-web'].deploy }}
    uses: cloudbees-days/preprod-hackers-web/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['preprod-hackers-web']['hackers-web'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['preprod-hackers-web']['hackers-web'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
